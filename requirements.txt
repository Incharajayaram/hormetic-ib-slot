# Core
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
tqdm>=4.65.0
PyYAML>=6.0
opencv-python>=4.7.0
Pillow>=9.0.0
pandas>=1.5.0

# Optional but recommended
# einops>=0.7.0          # not required, code uses pure torch ops
# wandb>=0.15.0          # not required, code logs to CSV
# torchvision>=0.15.0    # not required, transforms implemented manually

# For ARI computation
scikit-learn>=1.2.0
